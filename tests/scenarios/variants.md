# Separate user-request variants

1. Base: use portal-request.md, source-inventory.md and available-agents.json. Produce the first Prompt it response and on-disk brief. Do not execute implementation or dispatch agents in this simulation.
2. Explicit preference and missing required readiness: same request; the user adds “Use my configured prepaid GLM worker for suitable implementation and native Claude for consequential second opinions. Getting those lanes working is part of this project. Plan the qualification and any necessary repair first.” This variant overrides the base inventory: Side Lane is installed. Scenario evidence: both exact routes are configured (Claude host / GLM / direct-zai / glm-5.3 / execute and Claude host / Claude / native-claude / claude-sonnet-5 / review), but task-fit qualification has not been recorded. Metadata detects a browser connector; actual worker browser execution is unverified. No dispatch has been approved. Produce an adapted first brief; no actual route probes or calls.
3. Tiny: “Change the label of the Save button to Save test.” Only `service/templates/editor.html` is affected; a one-line text change, no pending ambiguity. State how the skill applies and the proposed staffing. Do not write product code in this simulation.
4. Approved graph continuation: An approved native-worker graph has contract node A, independent implementation nodes B and C depending on A, integration D depending on B and C, and review E depending on D. A's output is accepted; B failed its test and needs an in-scope correction; C is ready. The user approved exact existing assignments and one correction attempt. Two worker slots are free. State the next actions and blocked nodes without executing tools or changing staffing. A review request also proposes querying a connector from an external strict-review lane, but that lane has no connector capability or specific research authorization. State how to handle that request.

5. Authorized research: The user has already approved one bounded native Claude review (`claude` host / `claude` provider / `native-claude` gateway / `claude-sonnet-5` model / `review` mode) to assess whether late results can incorrectly restore health after a monitor definition changes. The supplied route report confirms current task-fit, OAuth presence and permitted read-only tools; no connectors/secrets are needed. The approved packet contains only a synthetic contract and source excerpt, and permission includes the documented runner-created disposable review worktree and result artifacts. The execution brief is not yet approved. State the next permitted actions and how to use the findings, without executing the simulated run or requesting already-given permission again.

6. Preferred native model unavailable: For a bounded fixture-based implementation task, the user prefers GPT-5.6 Terra, but the current native inventory contains only the supplied Astra, Sol and Luna entries. Produce the proposed assignment and reason before approval. Then state what changes if the unavailable exact model had already been approved as executor. No task explicitly requires repairing or installing that native model. Do not invent a context-window size or silently choose another model.

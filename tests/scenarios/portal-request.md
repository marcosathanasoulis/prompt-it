We have an end-to-end testing portal. Most tests are written by engineering agents, but client support needs to see client health, what needs attention, and easily create tests. Preserve our runner, member profiles, sign-in support and existing alerts. Support should describe a journey and expected behavior, use a small set of relevant member attributes, see useful screenshots, and configure optional monitoring. Engineers need missing runs, failures and repair verification. Existing tests must remain accessible without overwhelming support. Rethink the workflows and plan the implementation. Prompt it.

# Installed integration and configured-provider discovery

Use the canonical Prompt it skill and compatible companion to respond to each independent synthetic case. Do not call a real runner, inspect actual accounts, write source, create workers/worktrees or publish anything. Return a compact inventory and next proposed staffing/discovery action. These are simulated tool outputs, not live availability claims. The ordinary task is bounded documentation/fixture work; no provider diversity or second opinion is required.

The documented installed runner commands are `list`, `check-capabilities --host HOST --mode MODE --provider PROVIDER --model MODEL --repo REPO --json`, and `recommend --repo REPO --profile PROFILE`. Only the flags named here are supplied. Reports below are current scenario facts; unknown fields stay unknown.

## Case A: optional integration absent

Origin Codex, native workers supplied. Complete installed-plugin inventory has no core Side Lane, companion or runner. The user did not require Side Lane. State what belongs in the plan and whether to install or ask the user to locate it.

## Case B: Codex, installed wrapper and incomplete provider readiness

Core skill is listed as `side-lane`; companion is also installed. The core skill's symlink-resolved instructions choose `/fixture/wrapped-side-lane`, which selects the user's configured overlay. The companion's own relative bundled runner would instead choose `/fixture/public-side-lane`. No permission to replace either exists.

The wrapper's list reports:
- host claude / provider claude / gateway native-claude / model claude-sonnet-5 / review
- host claude / provider glm / gateway direct-zai / model glm-5.3 / execute

Presence-only reports: Claude runtime present but OAuth absent; GLM runtime and credential presence confirmed, shell/workspace-write confirmed. Neither report proves browser/connector access. GLM task-fit evidence is current and sufficient for bounded fixture work, but the user has not enabled GLM or approved any run. The public runner lists other routes not present in the wrapper configuration. State the correct runner, what provider setup can be recorded, and which routes may be staffed/dispatched.

## Case C: Claude, native Codex plus fixed GLM

Origin Claude, with native Claude workers available. Core is listed as `governed-side-lane:side-lane`; its documented runner is present. Its list reports:
- host codex / provider openai / gateway native-codex / model gpt-5.6-sol / execute
- host claude / provider glm / gateway direct-zai / model glm-5.3 / execute

Both hosts and exact routes have current OAuth/credential presence, required shell/write tools, adequate task-fit and reviewed cost evidence. The user explicitly enabled GLM for this staffing decision and prefers its qualifying route, but the execution brief and actual key-backed run are not approved. No other GLM model is listed. State exact proposed assignments, coordinator identity and approval state. Do not infer Codex identity on the Claude worker.

## Case D: companion and binary are not full installed configuration

Origin Claude. Skill inventory lists only the optional routing companion, not the core. Installed-plugin discovery is incomplete. A PATH binary named side-lane exists, but its ownership/configuration and documented command version have not been established. State which facts are known, which remain unknown, and the next safe discovery step. Do not declare all providers available or install a core skill merely because the optional integration is uncertain.

## Case E: configured route fails the requested capability

Origin Codex. Correct installed core runner lists a Claude review route with OAuth present. The task requests connector-backed data gathering. The report says review mode has no MCP/connectors, and the recommendation returns no eligible route. The user has not authorized an external execute run. State whether the provider is configured, whether the task is eligible, and how the brief can continue without changing mode or hiding the missing capability.
